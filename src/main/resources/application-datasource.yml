spring:
  config:
    activate:
      on-profile: "datasource"
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://${DB_HOST}:3306/ourhood_db
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}